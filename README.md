# Workshop-CustomVision

# On Windows
python -m venv venv

.\venv\Scripts\activate

pip install -r requirements.txt

# On Linux, WSL or macOS
python -m venv venv

source venv/bin/activate

pip install -r requirements.txt
